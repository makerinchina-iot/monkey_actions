
import { VerticalBox, HorizontalBox, Switch, Palette } from "std-widgets.slint";
export component PageSettings inherits VerticalLayout {
    // alignment: start;
    Rectangle {
        Text {
            text: "App settings page";
        }
    }
}
