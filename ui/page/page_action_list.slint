
import { VerticalBox } from "std-widgets.slint";
export component PageActionList inherits VerticalLayout {
    // alignment: start;
    Rectangle {
        Text {
            text: "Actions List Page";
        }
    }
}
